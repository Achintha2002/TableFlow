/// App-wide constants shared across the TableFlow mobile app.
class AppConstants {
  AppConstants._();

  /// Base URL for the Express backend.
  /// Change to your production URL before deploying.
  static const String backendUrl = 'http://localhost:3000';
}
